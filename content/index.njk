---
layout: layouts/home.njk
eleventyNavigation:
  key: Home
  order: 1
numberOfLatestPostsToShow: 8
---
<h1>COVER Magazine: The Underground National</h1>
    <p>Poet Jeffrey Cyphers Wright launched <em>Cover Magazine</em> in 1987. Its contributors included the era's vibrant local talent: Molly Jong-Fast, Timothy Greenfield-Sanders, David Ulin, and many more artists and writers covering their culture. The 64-page monthly journal circulated nationally until 2000. The complete set of <em>Cover</em> issues are archived at NYU Fales Library and at MoMA. Learn more on the About page and peruse the articles as we add them (below).</p>

{# {% set postsCount = collections.posts | length %}
{% set latestPostsCount = postsCount | min(numberOfLatestPostsToShow) %}
<h2>Latest {{ latestPostsCount }} Article{% if latestPostsCount != 1 %}s{% endif %}</h2>

{% set postslist = collections.posts | head(-1 * numberOfLatestPostsToShow) %}
{% set postslistCounter = postsCount %}
{% include "postslist.njk" %}

{% set morePosts = postsCount - numberOfLatestPostsToShow %}
{% if morePosts > 0 %}
<p>{{ morePosts }} more post{% if morePosts != 1 %}s{% endif %} can be found in <a href="/articles/">the archive</a>.</p>
{% endif %} #}

{# List every content page in the project #}
{#
<ul>
	{%- for entry in collections.all %}
	<li><a href="{{ entry.url }}"><code>{{ entry.url }}</code></a></li>
	{%- endfor %}
</ul>
#}

<div class="gallery" aria-labelledby="h2">
  <header>
    <h2>Articles</h2>
  </header>
  <ul class="articles-menu">
    {% for articles in collections.articles %}
      <li>
        <a href= "{{ articles.data.url }}"> 
          {# <div style="position:relative;overflow:hidden"> #}
         {#  {% image {{ articles.data.img_path }}, {{ articles.data.alt }} %} #}
            <img src={{ articles.data.img_path }} alt={{ articles.data.alt }}>
          {# </div> #}
          <h3>{{ articles.data.title}}</h3>
          <h4>{{ articles.data.issue }}</h4>
          <p>{{ articles.data.short_desc }}</p>
        </a>
      </li>
    {% endfor %}
</ul>
</div>